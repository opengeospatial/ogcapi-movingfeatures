description: A temporal property data that represents a time-to-acceleration curve of specified temporal primitive geometry.
content:
  application/json:
    schema:
      $ref: "../schemas/temporalProperty.yaml"
    example:
      $ref: "../examples/accelerationQuery.json"