description: A list of static data of moving feature.
content:
  application/json:
    schema:
      $ref: "../schemas/movingFeatureCollection.yaml"
    example:
      $ref: "../examples/movingFeatureCollection.json"