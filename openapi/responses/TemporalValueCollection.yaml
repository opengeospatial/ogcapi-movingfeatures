description: A list of Temporal Value data.
content:
  application/json:
    schema:
      $ref: "../schemas/temporalValueCollection.yaml"
    example:
      $ref: "../examples/temporalValueCollection.json"