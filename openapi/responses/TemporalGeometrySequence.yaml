description: A TemporalGeometrySequence data.
content:
  application/json:
    schema:
      $ref: "../schemas/temporalGeometrySequence.yaml"
    example:
      $ref: "../examples/temporalGeometrySequence.json"