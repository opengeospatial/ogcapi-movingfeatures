type: object
required:
  - temporalPropertiesCollection
properties:
  temporalPropertiesCollection:
    type: array
    items:
      $ref: temporalProperties.yaml
  links:
    type: array
    items:
      $ref: link.yaml
  timeStamp:
    type: string
    format: date-time
  numberMatched:
    type: integer
    minimum: 0
  numberReturned:
    type: integer
    minimum: 0